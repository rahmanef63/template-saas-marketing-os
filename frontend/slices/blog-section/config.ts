import { defineFeature } from "@/lib/shared/features/defineFeature";

export const blogSectionFeature = defineFeature({
  slug: "blog-section",
  title: "Blog Section",
  category: "ui",
  routes: [],
  nav: { label: "Blog Section", group: "ui", order: 82 },
});
